import React from 'react'
import AddProduct from '../addProduct/AddProduct'

const AddCategory = ({corner,stringData,pageType}) => {
  return (
    <AddProduct corner={corner} stringData={stringData} pageType={pageType} />

  )
}

export default AddCategory
